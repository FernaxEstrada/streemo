export const metadata = {
  title: 'Inicio de sesión | Streemo'
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return (
    <section>
      {children}
    </section>
  )
}
